sudo: required
services:
    - docker

before_install:
    - docker build -t jonbelinfante/docker-multi-container3 -f ./client/Dockerfile.dev .

script:
        - docker run -e CI=true jonbelinfante/docker-multi-comtainer3 npm run test


